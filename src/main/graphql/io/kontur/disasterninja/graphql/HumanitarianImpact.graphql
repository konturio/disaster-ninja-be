query HumanitarianImpact($polygon: GeoJSON) {
    polygonStatistic(polygonStatisticRequest:{
        polygonV2: $polygon
    }
    ){
        analytics {
            humanitarianImpact
        }
    }
}